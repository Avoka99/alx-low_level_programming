#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two digits
 * @a: first digit
 * @b: second digit
 *
 * Return: Multiplication of the digits
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);

}
